These files are python notebook files. To access these you have to download jupyter notebook.

For windows:
Type in CMD

pip3(or pip for Python2) install jupyter
and after installation just open cmd in the directory where downloaded files are save and type jupyter notebook.

For Linux
sudo apt-get -y install ipython ipython-notebook
sudo -H pip install jupyter

and after installing type ipython notebook in the bash .